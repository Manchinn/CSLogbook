# คู่มือการทดสอบ Teacher Types

## 🎯 วัตถุประสงค์การทดสอบ

ทดสอบความแตกต่างระหว่าง:
- **อาจารย์สายวิชาการ** (`teacherType: 'academic'`)
- **เจ้าหน้าที่ภาควิชา** (`teacherType: 'support'`)

---

## 📋 ขั้นตอนการทดสอบ

### 1. เตรียมข้อมูลทดสอบ

```bash
cd backend
node scripts/createTestTeachers.js
```

### 2. รัน Migration (หากยังไม่ได้รัน)

```bash
npm run migrate
```

### 3. รีสตาร์ท Backend Server

```bash
npm run dev
```

### 4. ทดสอบ API (Automated)

```bash
node scripts/testTeacherAPIs.js
```

---

## 🧪 การทดสอบแบบ Manual

### 📚 ทดสอบอาจารย์สายวิชาการ (Academic)

#### 1. เข้าสู่ระบบ
- **Username:** `academic_teacher`
- **Password:** `password123`

#### 2. ตรวจสอบเมนูที่ควรเห็น
✅ **เมนูที่ควรมี:**
- อาจารย์สายวิชาการ
  - ตรวจสอบเอกสาร
  - นักศึกษาในที่ปรึกษา
  - อนุมัติหัวข้อโครงงาน
  - ประเมินผลการฝึกงาน

❌ **เมนูที่ควรไม่มี:**
- ผู้ดูแลระบบ
  - จัดการผู้ใช้
  - จัดการเอกสาร
  - ตั้งค่าระบบ
  - รายงานสถิติ
  - ประกาศและแจ้งเตือน

#### 3. ทดสอบการเข้าถึงหน้า
✅ **หน้าที่ควรเข้าถึงได้:**
- `/teacher/review-documents`
- `/teacher/advising`
- `/teacher/project-approval`
- `/teacher/evaluation`

❌ **หน้าที่ควรถูกปฏิเสธ:**
- `/admin/users`
- `/admin/documents`
- `/admin/settings`
- `/admin/reports`

---

### 👨‍💼 ทดสอบเจ้าหน้าที่ภาควิชา (Support)

#### 1. เข้าสู่ระบบ
- **Username:** `support_staff`
- **Password:** `password123`

#### 2. ตรวจสอบเมนูที่ควรเห็น
✅ **เมนูที่ควรมี:**
- ผู้ดูแลระบบ
  - จัดการผู้ใช้
    - นักศึกษา
    - อาจารย์
  - จัดการเอกสาร
  - ตั้งค่าระบบ
  - รายงานสถิติ
  - ประกาศและแจ้งเตือน

❌ **เมนูที่ควรไม่มี:**
- อาจารย์สายวิชาการ
  - ตรวจสอบเอกสาร
  - นักศึกษาในที่ปรึกษา
  - อนุมัติหัวข้อโครงงาน
  - ประเมินผลการฝึกงาน

#### 3. ทดสอบการเข้าถึงหน้า
✅ **หน้าที่ควรเข้าถึงได้:**
- `/admin/users/students`
- `/admin/users/teachers`
- `/admin/documents`
- `/admin/settings`
- `/admin/reports`
- `/admin/announcements`

❌ **หน้าที่ควรถูกปฏิเสธ:**
- `/teacher/review-documents`
- `/teacher/advising`
- `/teacher/project-approval`
- `/teacher/evaluation`

---

## 🔍 การตรวจสอบใน Developer Tools

### 1. ตรวจสอบ Local Storage
เปิด Developer Tools → Application → Local Storage:

**อาจารย์สายวิชาการ:**
```javascript
{
  "role": "teacher",
  "teacherType": "academic"
}
```

**เจ้าหน้าที่ภาควิชา:**
```javascript
{
  "role": "teacher", 
  "teacherType": "support"
}
```

### 2. ตรวจสอบ Network Requests
ดู API calls ใน Network tab:

**อาจารย์สายวิชาการ:**
- ✅ `GET /api/teachers/academic/dashboard`
- ✅ `POST /api/teachers/academic/evaluation`
- ❌ `GET /api/teachers/support/dashboard` (403 Forbidden)

**เจ้าหน้าที่ภาควิชา:**
- ✅ `GET /api/teachers/support/dashboard`
- ✅ `POST /api/teachers/support/announcement`
- ❌ `GET /api/teachers/academic/dashboard` (403 Forbidden)

---

## 🐛 การแก้ไขปัญหา

### ปัญหาที่อาจเกิดขึ้น:

#### 1. เข้าสู่ระบบไม่ได้
**สาเหตุ:** ข้อมูลทดสอบยังไม่ได้สร้าง
**แก้ไข:** รัน `node scripts/createTestTeachers.js`

#### 2. เมนูไม่แสดงตามที่คาดหวัง
**สาเหตุ:** AuthContext ไม่ได้อัปเดต
**แก้ไข:** ตรวจสอบ localStorage และ refresh หน้า

#### 3. API ถูกปฏิเสธ (403)
**สาเหตุ:** Middleware ไม่ทำงาน
**แก้ไข:** ตรวจสอบ backend logs และ middleware

#### 4. หน้าแสดง 404
**สาเหตุ:** Routes ยังไม่ได้สร้าง
**แก้ไข:** ตรวจสอบ frontend routes ใน App.js

---

## 📊 ผลลัพธ์ที่คาดหวัง

### ✅ การทดสอบที่ควรผ่าน:

1. **การเข้าสู่ระบบ**
   - ทั้งสองประเภทเข้าสู่ระบบได้
   - ข้อมูลถูกเก็บใน localStorage

2. **การแสดงเมนู**
   - Academic: เห็นเมนูอาจารย์สายวิชาการ
   - Support: เห็นเมนูผู้ดูแลระบบ

3. **การเข้าถึงหน้า**
   - Academic: เข้าถึงหน้า teacher ได้
   - Support: เข้าถึงหน้า admin ได้

4. **การป้องกันการเข้าถึง**
   - Academic: ไม่สามารถเข้าถึงหน้า admin
   - Support: ไม่สามารถเข้าถึงหน้า teacher

5. **API Authorization**
   - Academic: เข้าถึง academic APIs ได้
   - Support: เข้าถึง support APIs ได้
   - ทั้งสอง: ไม่สามารถเข้าถึง APIs ของอีกประเภท

---

## 🎯 สิ่งที่ต้องทดสอบเพิ่มเติม

### 1. การแปลงข้อมูล Admin
```bash
node scripts/checkTeacherTypes.js
```

### 2. การทำงานร่วมกับระบบเดิม
- ทดสอบการเข้าสู่ระบบด้วย admin ที่มีอยู่
- ตรวจสอบว่าถูกแปลงเป็น teacher support หรือไม่

### 3. การจัดการข้อมูล
- ทดสอบการสร้าง teacher ใหม่
- ทดสอบการอัปเดต teacher type

---

## 📝 บันทึกผลการทดสอบ

สร้างไฟล์ `TEST_RESULTS.md` เพื่อบันทึกผลการทดสอบ:

```markdown
# ผลการทดสอบ Teacher Types

## วันที่ทดสอบ: [วันที่]

### ✅ การทดสอบที่ผ่าน:
- [ ] การเข้าสู่ระบบ
- [ ] การแสดงเมนู
- [ ] การเข้าถึงหน้า
- [ ] การป้องกันการเข้าถึง
- [ ] API Authorization

### ❌ การทดสอบที่ล้มเหลว:
- [ ] ปัญหา:
- [ ] วิธีแก้ไข:

### 🔧 ปัญหาที่พบ:
- [ ] ปัญหา:
- [ ] สาเหตุ:
- [ ] การแก้ไข:

### 📋 หมายเหตุ:
- [ ] ข้อสังเกต:
- [ ] ข้อเสนอแนะ:
```

---

## 🚀 ขั้นตอนต่อไป

หลังจากทดสอบเสร็จแล้ว:

1. **แก้ไขปัญหา** ที่พบ
2. **เพิ่มลอจิกจริง** แทน placeholder functions
3. **สร้าง UI Components** สำหรับแต่ละประเภท
4. **ทดสอบการใช้งานจริง** กับผู้ใช้จริง 