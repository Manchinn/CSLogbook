# แก้ไขปัญหา Support Staff Menu ใน Sidebar.js

## 🐛 ปัญหาที่พบ

### ปัญหาหลัก:
`support_staff` ควรเห็นเมนู admin แต่ตอนนี้เห็นเมนู academic teacher แทน

### สาเหตุ:
ใน `frontend/src/components/layout/Sidebar.js` ยังใช้เงื่อนไขเก่า:
```javascript
userData?.role === "teacher"
```
ไม่ได้แยกตาม `teacherType`

---

## ✅ การแก้ไขที่ทำ

### 1. แก้ไข Sidebar.js

#### ก่อนแก้ไข:
```javascript
// Teacher Menu Items
...(userData?.role === "teacher"
  ? [
      {
        key: "/review-documents",
        icon: <FileTextOutlined />,
        label: "ตรวจสอบเอกสารโครงงาน",
      },
      // ... academic teacher menus
    ]
  : []),
```

#### หลังแก้ไข:
```javascript
// Teacher Menu Items - Academic (อาจารย์สายวิชาการ)
...(userData?.role === "teacher" && userData?.teacherType === "academic"
  ? [
      {
        key: "/review-documents",
        icon: <FileTextOutlined />,
        label: "ตรวจสอบเอกสารโครงงาน",
      },
      // ... academic teacher menus
    ]
  : []),

// Teacher Menu Items - Support (เจ้าหน้าที่ภาควิชา)
...(userData?.role === "teacher" && userData?.teacherType === "support"
  ? [
      {
        key: "manage",
        icon: <TeamOutlined />,
        label: "จัดการข้อมูล",
        children: [
          {
            key: "/admin/users/students",
            label: "นักศึกษา",
          },
          {
            key: "/admin/users/teachers",
            label: "อาจารย์",
          },
          // ... admin menus
        ],
      },
      // ... support teacher menus
    ]
  : []),
```

### 2. อัปเดต Badge แสดง Role

#### ก่อนแก้ไข:
```javascript
count={
  !userData?.role
    ? ""
    : userData.role === "admin"
    ? "ผู้ดูแลระบบ"
    : userData.role === "teacher"
    ? "อาจารย์"
    : "นักศึกษา"
}
```

#### หลังแก้ไข:
```javascript
count={
  !userData?.role
    ? ""
    : userData.role === "admin"
    ? "ผู้ดูแลระบบ"
    : userData.role === "teacher"
    ? userData.teacherType === "support"
      ? "เจ้าหน้าที่ภาควิชา"
      : userData.teacherType === "academic"
      ? "อาจารย์สายวิชาการ"
      : "อาจารย์"
    : "นักศึกษา"
}
```

---

## 🎯 ผลลัพธ์ที่คาดหวัง

### ✅ หลังแก้ไข:

#### สำหรับ `support_staff` (teacherType: 'support'):
- **Badge**: แสดง "เจ้าหน้าที่ภาควิชา"
- **เมนูที่ควรเห็น**:
  - จัดการข้อมูล
    - นักศึกษา
    - อาจารย์
    - คู่โปรเจค
  - จัดการเอกสาร
    - เอกสารฝึกงาน
    - เอกสารโครงงานพิเศษ
  - อัปโหลดรายชื่อนักศึกษา
  - ตั้งค่าระบบ
    - ภาพรวมการตั้งค่า
    - หลักสูตรการศึกษา
    - ปีการศึกษา/ภาคเรียน
    - สถานะนักศึกษา
    - การแจ้งเตือน
    - ขั้นตอนการทำงาน

#### สำหรับ `academic_teacher` (teacherType: 'academic'):
- **Badge**: แสดง "อาจารย์สายวิชาการ"
- **เมนูที่ควรเห็น**:
  - ตรวจสอบเอกสารโครงงาน
  - ให้คำแนะนำโครงงาน
  - อนุมัติเอกสาร

---

## 🧪 การทดสอบ

### 1. รันสคริปต์ทดสอบ:
```bash
cd backend
node scripts/testSupportStaffFix.js
```

### 2. ทดสอบ Frontend:
1. รีสตาร์ท backend server
2. เข้าสู่ระบบด้วย `support_staff` / `password123`
3. ตรวจสอบเมนูที่แสดงใน sidebar
4. ตรวจสอบ Badge ที่แสดง

### 3. ตรวจสอบ localStorage:
```javascript
// เปิด Developer Tools → Console
console.log('Role:', localStorage.getItem('role'));
console.log('TeacherType:', localStorage.getItem('teacherType'));
```

---

## 📋 ไฟล์ที่แก้ไข

### Frontend:
1. `frontend/src/components/layout/Sidebar.js` - แก้ไขเงื่อนไขการแสดงเมนูและ Badge

### Backend:
1. `backend/scripts/testSupportStaffFix.js` - สคริปต์ทดสอบการแก้ไข

---

## 🚀 ขั้นตอนการแก้ไข

### 1. แก้ไขข้อมูลในฐานข้อมูล:
```bash
cd backend
node scripts/testSupportStaffFix.js
```

### 2. รีสตาร์ท backend server:
```bash
npm run dev
```

### 3. ทดสอบ Frontend:
- เข้าสู่ระบบด้วย `support_staff`
- ตรวจสอบเมนูที่แสดง
- ตรวจสอบ Badge ที่แสดง

---

## 🎉 สรุป

**ปัญหาได้รับการแก้ไขเรียบร้อยแล้ว!**

การแก้ไขหลัก:
1. ✅ แยกเมนูตาม `teacherType` ใน Sidebar.js
2. ✅ อัปเดต Badge ให้แสดงประเภทที่ถูกต้อง
3. ✅ สร้างสคริปต์ทดสอบการแก้ไข

ตอนนี้ `support_staff` ควรเห็นเมนู admin ตามที่คาดหวังแล้วครับ! 🚀
