# แก้ไขปัญหา Support Staff Menu

## 🐛 ปัญหาที่พบ

### ปัญหาหลัก:
`support_staff` ควรเห็นเมนู admin แต่ตอนนี้เห็นเมนู academic teacher แทน

### สาเหตุที่คาดการณ์:
1. **ข้อมูลในฐานข้อมูล**: `support_staff` อาจมี `teacherType: 'academic'` แทนที่จะเป็น `'support'`
2. **การส่งข้อมูล**: `teacherType` อาจไม่ถูกส่งมาใน JWT token หรือ localStorage
3. **Frontend Logic**: เมนูอาจไม่ถูกต้องตามเงื่อนไข

---

## 🔍 การตรวจสอบ

### 1. ตรวจสอบข้อมูลในฐานข้อมูล
```bash
cd backend
node scripts/checkSupportStaff.js
```

### 2. แก้ไขข้อมูลในฐานข้อมูล (หากจำเป็น)
```bash
cd backend
node scripts/fixSupportStaffDirect.js
```

### 3. ทดสอบการเข้าสู่ระบบ
```bash
cd backend
node scripts/testLoginWithTeacherType.js
```

---

## ✅ วิธีแก้ไข

### ขั้นตอนที่ 1: ตรวจสอบและแก้ไขข้อมูล

1. **ตรวจสอบข้อมูลปัจจุบัน:**
   ```sql
   SELECT u.username, u.role, t.teacher_type
   FROM users u
   LEFT JOIN teachers t ON u.user_id = t.user_id
   WHERE u.username = 'support_staff'
   ```

2. **แก้ไข teacherType (หากจำเป็น):**
   ```sql
   UPDATE teachers 
   SET teacher_type = 'support' 
   WHERE user_id = (SELECT user_id FROM users WHERE username = 'support_staff')
   ```

### ขั้นตอนที่ 2: ตรวจสอบ Frontend

1. **ตรวจสอบ localStorage:**
   - เปิด Developer Tools → Application → Local Storage
   - ตรวจสอบว่า `teacherType` มีค่าเป็น `'support'` หรือไม่

2. **ตรวจสอบ JWT Token:**
   - เปิด Developer Tools → Network tab
   - ดู response ของ `/api/auth/login`
   - ตรวจสอบว่า `teacherType` ถูกส่งมาหรือไม่

### ขั้นตอนที่ 3: ทดสอบการทำงาน

1. **เข้าสู่ระบบด้วย support_staff:**
   - Username: `support_staff`
   - Password: `password123`

2. **ตรวจสอบเมนูที่ควรเห็น:**
   ✅ **เมนูที่ควรมี:**
   - ผู้ดูแลระบบ
     - จัดการผู้ใช้
       - นักศึกษา
       - อาจารย์
     - จัดการเอกสาร
     - ตั้งค่าระบบ
     - รายงานสถิติ
     - ประกาศและแจ้งเตือน

   ❌ **เมนูที่ควรไม่มี:**
   - อาจารย์สายวิชาการ
     - ตรวจสอบเอกสาร
     - นักศึกษาในที่ปรึกษา
     - อนุมัติหัวข้อโครงงาน
     - ประเมินผลการฝึกงาน

---

## 🧪 การทดสอบ

### 1. ทดสอบ Backend API
```bash
cd backend
node scripts/testTeacherAPIs.js
```

### 2. ทดสอบ Frontend
```javascript
// เปิด Developer Tools → Console
// เรียกใช้ฟังก์ชันทดสอบ
testTeacherAccess();
```

### 3. ทดสอบการเข้าสู่ระบบ
```bash
cd backend
node scripts/testLoginWithTeacherType.js
```

---

## 📋 ไฟล์ที่เกี่ยวข้อง

### Backend:
1. `backend/services/authService.js` - ส่ง teacherType ใน JWT
2. `backend/controllers/authController.js` - ส่ง teacherType ใน response
3. `backend/middleware/authMiddleware.js` - ตรวจสอบ teacherType

### Frontend:
1. `frontend/src/contexts/AuthContext.js` - เก็บ teacherType ใน localStorage
2. `frontend/src/components/layout/menuConfig.js` - แสดงเมนูตาม teacherType
3. `frontend/src/App.js` - ProtectedRoute สำหรับ teacherType

---

## 🎯 ผลลัพธ์ที่คาดหวัง

### ✅ หลังแก้ไข:
- `support_staff` ควรเห็นเมนู "ผู้ดูแลระบบ"
- `academic_teacher` ควรเห็นเมนู "อาจารย์สายวิชาการ"
- การเข้าถึงหน้า admin ควรทำงานได้สำหรับ support_staff
- การเข้าถึงหน้า teacher ควรทำงานได้สำหรับ academic_teacher

### 🔍 สิ่งที่ต้องตรวจสอบ:
1. ข้อมูลในฐานข้อมูลถูกต้อง
2. JWT token มี teacherType
3. localStorage มี teacherType
4. เมนูแสดงตามเงื่อนไขที่ถูกต้อง

---

## 🚀 ขั้นตอนการแก้ไข

1. **รันสคริปต์ตรวจสอบ:**
   ```bash
   cd backend
   node scripts/checkSupportStaff.js
   ```

2. **แก้ไขข้อมูล (หากจำเป็น):**
   ```bash
   cd backend
   node scripts/fixSupportStaffDirect.js
   ```

3. **ทดสอบการเข้าสู่ระบบ:**
   ```bash
   cd backend
   node scripts/testLoginWithTeacherType.js
   ```

4. **ทดสอบ Frontend:**
   - เข้าสู่ระบบด้วย `support_staff`
   - ตรวจสอบเมนูที่แสดง
   - ตรวจสอบ localStorage

---

## 🎉 สรุป

ปัญหาน่าจะเกิดจากข้อมูลในฐานข้อมูลที่ `support_staff` มี `teacherType: 'academic'` แทนที่จะเป็น `'support'` 

หลังจากแก้ไขข้อมูลแล้ว `support_staff` ควรเห็นเมนู admin ตามที่คาดหวังครับ! 🚀
