# ===========================================
# CSLogbook Production Environment
# ===========================================

# พอร์ตรับเข้า (host:container)
BACKEND_PORT=5000
FRONTEND_PORT=3000
MYSQL_PORT=3307

# การเชื่อมต่อฐานข้อมูล (MySQL service ใน Docker network)
DB_HOST=mysql
DB_PORT=3306
DB_NAME=cslogbook_prod
DB_USER=cslogbook_prod
DB_PASSWORD=YourSecurePassword123!
MYSQL_ROOT_PASSWORD=YourRootPassword456!

# การตั้งค่า Backend
NODE_ENV=production
BASE_URL=https://api.yourdomain.com
API_PREFIX=/api
FRONTEND_URL=https://cslogbook.yourdomain.com
PORT=5000
JWT_SECRET=YourVerySecureJWTSecret64CharactersLongForProductionUse!
JWT_EXPIRES_IN=1d
UPLOAD_DIR=uploads
MAX_FILE_SIZE=5242880
LOG_ENABLE_CONSOLE=true

# อีเมล (SendGrid Configuration)
SENDGRID_API_KEY=SG.your-sendgrid-api-key-here
EMAIL_PROVIDER=sendgrid
EMAIL_SENDER=no-reply@yourdomain.com
EMAIL_LOGIN_ENABLED=true
EMAIL_DOCUMENT_ENABLED=true
EMAIL_LOGBOOK_ENABLED=true
EMAIL_MEETING_ENABLED=true

# Academic Auto Update
ACADEMIC_AUTO_UPDATE_ENABLED=true
ACADEMIC_AUTO_UPDATE_CRON=5 0 * * *

# ค่า Build สำหรับ Frontend (มุมมองของ Browser)
REACT_APP_API_URL=https://api.yourdomain.com/api
REACT_APP_UPLOAD_URL=https://api.yourdomain.com/uploads

# ===========================================
# ค่าที่ต้องแก้ไขให้ตรงกับ Production:
# ===========================================
# 1. DB_PASSWORD - รหัสผ่านฐานข้อมูลที่ปลอดภัย
# 2. MYSQL_ROOT_PASSWORD - รหัสผ่าน root MySQL
# 3. JWT_SECRET - JWT secret ที่ปลอดภัย (64+ characters)
# 4. BASE_URL - URL ของ Backend API
# 5. FRONTEND_URL - URL ของ Frontend
# 6. SENDGRID_API_KEY - API Key จาก SendGrid
# 7. EMAIL_SENDER - อีเมลผู้ส่ง
# 8. REACT_APP_API_URL - API URL สำหรับ Frontend
# 9. REACT_APP_UPLOAD_URL - Upload URL สำหรับ Frontend