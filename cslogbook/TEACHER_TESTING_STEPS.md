# คู่มือการทดสอบ Teacher Types แบบ Step-by-Step

## 🎯 วัตถุประสงค์
ทดสอบว่าระบบรองรับการแยกประเภท teacher เป็น academic และ support ได้อย่างถูกต้อง

---

## 📋 ขั้นตอนการทดสอบ

### 1. เตรียมระบบ

```bash
# รัน backend server
cd backend
npm run dev

# รัน frontend server (ใน terminal อีกตัว)
cd frontend
npm start
```

### 2. ตรวจสอบข้อมูลทดสอบ

```bash
cd backend
node scripts/checkTeacherTypes.js
```

**ผลลัพธ์ที่คาดหวัง:**
- พบ Academic Teachers
- พบ Support Teachers
- ไม่มี Admin ที่เหลือ

### 3. ทดสอบ API Backend

```bash
cd backend
node scripts/testTeacherAPIs.js
```

**ผลลัพธ์ที่คาดหวัง:**
- ✅ Academic Teacher: เข้าสู่ระบบสำเร็จ, เข้าถึง academic APIs ได้, ถูกปฏิเสธ support APIs
- ✅ Support Teacher: เข้าสู่ระบบสำเร็จ, เข้าถึง support APIs ได้, ถูกปฏิเสธ academic APIs

---

## 🧪 การทดสอบ Frontend

### Step 1: ทดสอบ Academic Teacher

#### 1.1 เข้าสู่ระบบ
- เปิดเบราว์เซอร์ไปที่ `http://localhost:3000`
- เข้าสู่ระบบด้วย:
  - **Username:** `academic_teacher`
  - **Password:** `password123`

#### 1.2 ตรวจสอบเมนู
✅ **เมนูที่ควรเห็น:**
- อาจารย์สายวิชาการ
  - ตรวจสอบเอกสาร
  - นักศึกษาในที่ปรึกษา
  - อนุมัติหัวข้อโครงงาน
  - ประเมินผลการฝึกงาน

❌ **เมนูที่ควรไม่มี:**
- ผู้ดูแลระบบ
  - จัดการผู้ใช้
  - จัดการเอกสาร
  - ตั้งค่าระบบ
  - รายงานสถิติ

#### 1.3 ตรวจสอบ Local Storage
เปิด Developer Tools → Application → Local Storage:
```javascript
{
  "role": "teacher",
  "teacherType": "academic"
}
```

#### 1.4 ทดสอบการเข้าถึงหน้า
✅ **หน้าที่ควรเข้าถึงได้:**
- `/teacher/review-documents`
- `/teacher/advising`
- `/teacher/project-approval`
- `/teacher/evaluation`

❌ **หน้าที่ควรถูกปฏิเสธ:**
- `/admin/users`
- `/admin/documents`
- `/admin/settings`
- `/admin/reports`

#### 1.5 ทดสอบ API Calls
เปิด Developer Tools → Network tab:
✅ **API ที่ควรเรียกได้:**
- `GET /api/teachers/academic/dashboard`
- `POST /api/teachers/academic/evaluation`
- `GET /api/teachers/documents`

❌ **API ที่ควรถูกปฏิเสธ:**
- `GET /api/teachers/support/dashboard`
- `POST /api/teachers/support/announcement`

---

### Step 2: ทดสอบ Support Teacher

#### 2.1 เข้าสู่ระบบ
- ออกจากระบบ
- เข้าสู่ระบบด้วย:
  - **Username:** `support_staff`
  - **Password:** `password123`

#### 2.2 ตรวจสอบเมนู
✅ **เมนูที่ควรเห็น:**
- ผู้ดูแลระบบ
  - จัดการผู้ใช้
    - นักศึกษา
    - อาจารย์
  - จัดการเอกสาร
  - ตั้งค่าระบบ
  - รายงานสถิติ
  - ประกาศและแจ้งเตือน

❌ **เมนูที่ควรไม่มี:**
- อาจารย์สายวิชาการ
  - ตรวจสอบเอกสาร
  - นักศึกษาในที่ปรึกษา
  - อนุมัติหัวข้อโครงงาน
  - ประเมินผลการฝึกงาน

#### 2.3 ตรวจสอบ Local Storage
```javascript
{
  "role": "teacher",
  "teacherType": "support"
}
```

#### 2.4 ทดสอบการเข้าถึงหน้า
✅ **หน้าที่ควรเข้าถึงได้:**
- `/admin/users/students`
- `/admin/users/teachers`
- `/admin/documents`
- `/admin/settings`
- `/admin/reports`
- `/admin/announcements`

❌ **หน้าที่ควรถูกปฏิเสธ:**
- `/teacher/review-documents`
- `/teacher/advising`
- `/teacher/project-approval`
- `/teacher/evaluation`

#### 2.5 ทดสอบ API Calls
✅ **API ที่ควรเรียกได้:**
- `GET /api/teachers/support/dashboard`
- `POST /api/teachers/support/announcement`
- `GET /api/teachers/documents`

❌ **API ที่ควรถูกปฏิเสธ:**
- `GET /api/teachers/academic/dashboard`
- `POST /api/teachers/academic/evaluation`

---

## 🔍 การตรวจสอบเพิ่มเติม

### 1. ทดสอบการแปลง Admin
```bash
cd backend
node scripts/checkTeacherTypes.js
```

ตรวจสอบว่า admin เดิมถูกแปลงเป็น teacher support หรือไม่

### 2. ทดสอบการสร้าง Teacher ใหม่
- เข้าสู่ระบบด้วย admin/support
- ไปที่ จัดการผู้ใช้ → อาจารย์
- สร้างอาจารย์ใหม่
- ตรวจสอบว่า teacherType ถูกกำหนดหรือไม่

### 3. ทดสอบการอัปเดต Teacher Type
- แก้ไขข้อมูลอาจารย์
- เปลี่ยน teacherType
- ตรวจสอบว่าการเข้าถึงเปลี่ยนตามหรือไม่

---

## 🐛 การแก้ไขปัญหา

### ปัญหา: เข้าสู่ระบบไม่ได้
**สาเหตุ:** ข้อมูลทดสอบไม่มี
**แก้ไข:** รัน `node scripts/createTestTeachers.js`

### ปัญหา: เมนูไม่แสดง
**สาเหตุ:** AuthContext ไม่ได้อัปเดต
**แก้ไข:** ตรวจสอบ localStorage และ refresh หน้า

### ปัญหา: หน้าแสดง 404
**สาเหตุ:** Routes ยังไม่ได้สร้าง
**แก้ไข:** ตรวจสอบ App.js และ AdminRoutes

### ปัญหา: API ถูกปฏิเสธ (403)
**สาเหตุ:** Middleware ไม่ทำงาน
**แก้ไข:** ตรวจสอบ backend logs

---

## 📊 บันทึกผลการทดสอบ

สร้างไฟล์ `TEST_RESULTS.md`:

```markdown
# ผลการทดสอบ Teacher Types

## วันที่ทดสอบ: [วันที่]

### ✅ การทดสอบที่ผ่าน:
- [ ] Academic Teacher เข้าสู่ระบบได้
- [ ] Academic Teacher เห็นเมนูถูกต้อง
- [ ] Academic Teacher เข้าถึงหน้าได้
- [ ] Academic Teacher ถูกปฏิเสธหน้าที่ไม่ควรเข้าถึง
- [ ] Support Teacher เข้าสู่ระบบได้
- [ ] Support Teacher เห็นเมนูถูกต้อง
- [ ] Support Teacher เข้าถึงหน้าได้
- [ ] Support Teacher ถูกปฏิเสธหน้าที่ไม่ควรเข้าถึง
- [ ] API Authorization ทำงานถูกต้อง

### ❌ การทดสอบที่ล้มเหลว:
- [ ] ปัญหา:
- [ ] วิธีแก้ไข:

### 🔧 ปัญหาที่พบ:
- [ ] ปัญหา:
- [ ] สาเหตุ:
- [ ] การแก้ไข:

### 📋 หมายเหตุ:
- [ ] ข้อสังเกต:
- [ ] ข้อเสนอแนะ:
```

---

## 🎉 ผลลัพธ์ที่คาดหวัง

### ✅ การทดสอบที่ควรผ่านทั้งหมด:

1. **การเข้าสู่ระบบ**
   - Academic Teacher เข้าสู่ระบบได้
   - Support Teacher เข้าสู่ระบบได้

2. **การแสดงเมนู**
   - Academic: เห็นเมนูอาจารย์สายวิชาการ
   - Support: เห็นเมนูผู้ดูแลระบบ

3. **การเข้าถึงหน้า**
   - Academic: เข้าถึงหน้า teacher ได้
   - Support: เข้าถึงหน้า admin ได้

4. **การป้องกันการเข้าถึง**
   - Academic: ไม่สามารถเข้าถึงหน้า admin
   - Support: ไม่สามารถเข้าถึงหน้า teacher

5. **API Authorization**
   - Academic: เข้าถึง academic APIs ได้
   - Support: เข้าถึง support APIs ได้
   - ทั้งสอง: ไม่สามารถเข้าถึง APIs ของอีกประเภท

---

## 🚀 ขั้นตอนต่อไป

หลังจากทดสอบเสร็จแล้ว:

1. **แก้ไขปัญหา** ที่พบ
2. **เพิ่มลอจิกจริง** แทน placeholder functions
3. **สร้าง UI Components** สำหรับแต่ละประเภท
4. **ทดสอบการใช้งานจริง** กับผู้ใช้จริง 