import React from 'react';
import { Typography } from 'antd';

const ProposalRevisionPage = () => {
  return <Typography.Paragraph>TODO: อัปโหลด Proposal เวอร์ชัน + แสดงประวัติ (เชื่อม service uploadProposal ที่มี)</Typography.Paragraph>;
};
export default ProposalRevisionPage;
